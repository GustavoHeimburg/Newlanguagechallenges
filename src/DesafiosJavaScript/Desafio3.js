desafio3 = ()=> {
    var num = prompt('Digite um Numero');

    for (i = 0; i <= num; i++) {
        if (i % 2 == 0) {
            console.log(i + " é par");
        }
        alert("Valores expressos no terminal.")
    }
}